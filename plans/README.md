# Implementation Plans

Generated by the improve skill on 2026-08-29 against commit `7fb24e5`
(main after PR #3 skill-stack merge). Executed non-interactively: top 5
findings by leverage selected automatically.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Wire skills-lock.json into install and CI verification | P1 | M | — | TODO |
| 002 | Pin the skills CLI adapter version in install script | P2 | S | — | TODO |
| 003 | Add Berd skill-stack profile for agent discovery | P2 | M | 001 | TODO |
| 004 | Resolve js-yaml high advisory in SDK dependency chain | P2 | S | — | TODO |
| 005 | Seed CONTEXT.md with Berd domain vocabulary | P3 | S | — | TODO |

## Dependency notes

- 003 benefits from 001 so skill hashes are trustworthy before curating a profile.
- 002 and 001 can land in either order; 001 is higher leverage.

## Findings considered and rejected

- **Commit all 741 skills to git is wrong**: by design for Cloud Agents; documented in `.agents/skills/README.md` and ADR candidate in plan 005.
- **cloud-agent-install.sh must run install-agent-skills**: skills are pre-vendored; install is for refresh only — no change needed.
- **Duplicate mattpocock vs Berd code-review skill**: intentional; protected list preserves Berd `code-review`.
- **Repo-wide `any` cleanup**: only one production `as any` in `acpApi.ts`; insufficient leverage for a plan this pass.
- **Merge open PR #2 cloud-agent-env**: separate workstream; not part of this audit.

## Audit scope notes

**Audited**: skill-stack infrastructure (`scripts/install-agent-skills.sh`, `skills-lock.json`, `docs/agents/`, `AGENTS.md`, `.cursor/environment.json`), berdctl bridge/registry, skills API surface, `pnpm audit`, `just check`.

**Not audited**: full `.agents/skills/` content (740 third-party skills), Rust clippy full run, E2E suite, Goose backend internals, bb-cli acceptance.
